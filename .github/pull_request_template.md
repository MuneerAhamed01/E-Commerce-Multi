## Summary
<!-- What phase/milestone is this? What changed and why? -->

- Phase:
- Milestone(s):

## Checklist
- [ ] Branched from `develop` as `phase/<N>-<short-name>` (or a short-lived fix branch off `develop`)
- [ ] No secrets committed (`*.secret.env`, Firebase credentials, keystores)
- [ ] `melos run format` / `analyze` / `test` pass locally
- [ ] `docs/14_IMPLEMENTATION_PROGRESS.md` updated if a milestone completed
- [ ] Targets `develop` (not `main`) unless this is a release merge
