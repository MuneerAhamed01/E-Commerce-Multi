name: search
resolution: workspace
description: >
  Query-driven product discovery with filters and sort, across the full catalog.
  See docs/04_FEATURE_IMPLEMENTATION_ORDER.md for the full specification.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.12.0 <4.0.0"
  flutter: ">=3.44.0"

dependencies:
  core: ^0.1.0
  design_system: ^0.1.0
  equatable: ^2.1.0
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  get_it: ^9.2.1
  go_router: ^17.3.0
  products: ^0.1.0
  shared_preferences: ^2.5.3

dev_dependencies:
  bloc_test: ^10.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4

flutter:
  uses-material-design: true
