# Copy to config/env/dev.secret.env (gitignored) and fill real values locally.
# Never commit *.secret.env — see docs/11_ENVIRONMENT_CONFIGURATION.md §4.
#
# Injected at build time via CI secrets or local --dart-define-from-file
# *after* the non-secret config/env/dev.env file.

SENTRY_DSN=
# FIREBASE_API_KEY=
# PAYMENT_GATEWAY_SECRET=
