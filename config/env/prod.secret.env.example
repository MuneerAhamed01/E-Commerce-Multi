# Copy to config/env/prod.secret.env (gitignored) and fill real values.
# Never commit *.secret.env — see docs/11_ENVIRONMENT_CONFIGURATION.md §4.
# Production secrets must come from the CI/CD secret store, not developer machines.

SENTRY_DSN=
# FIREBASE_API_KEY=
# PAYMENT_GATEWAY_SECRET=
