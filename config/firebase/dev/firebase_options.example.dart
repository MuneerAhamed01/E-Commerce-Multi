// EXAMPLE ONLY — copy to firebase_options.dart (gitignored) via FlutterFire CLI.
// Real credentials must never be committed. See docs/11_ENVIRONMENT_CONFIGURATION.md §9.
//
// ignore_for_file: type=lint

/// Placeholder shape matching FlutterFire's generated `firebase_options.dart`.
/// Replace with the real generated file when Firebase projects are provisioned.
class DefaultFirebaseOptions {
  static Never get currentPlatform => throw UnimplementedError(
        'Firebase is not configured for this environment. '
        'Run FlutterFire CLI and place the generated file at '
        'config/firebase/<env>/firebase_options.dart (gitignored).',
      );
}
